# Mushroom Environmental Control System Configuration

# Security
SECRET_KEY=your-secret-key-here-change-this
DASHBOARD_PASSWORD=admin123

# MongoDB Atlas Configuration (Cloud Database)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mushroom_db?retryWrites=true&w=majority

# Optional: Custom device identifier
DEVICE_ID=mushroom-control-01

# Optional: Custom sensor calibration offsets
TEMP_OFFSET=0.0
HUMIDITY_OFFSET=0.0
CO2_OFFSET=0
LIGHT_OFFSET=0

# Optional: Custom thresholds (uncomment to override config.py defaults)
# TEMP_MIN=18
# TEMP_MAX=24
# HUMIDITY_MIN=80
# HUMIDITY_MAX=95
# CO2_MIN=800
# CO2_MAX=1200